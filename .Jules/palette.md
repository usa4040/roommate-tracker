## 2024-05-23 - Modal Accessibility Pattern
**Learning:** Adding `role="dialog"` and `aria-modal="true"` to custom modal implementations is a critical pattern often missed when not using a UI library.
**Action:** Create a reusable `Modal` component to encapsulate these accessibility features and ensure consistency across the application.